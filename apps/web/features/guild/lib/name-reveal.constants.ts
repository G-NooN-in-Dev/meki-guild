/** localStorage에 저장하는 unlock 플래그 키 */
export const NAME_REVEAL_STORAGE_KEY = 'meki-guild-name-unlocked'

/**
 * 길드원에게만 공유하는 이름 공개 비밀번호.
 * 클라이언트에 포함되므로 “진짜 보안”이 아니라 대외용 소프트 가림용입니다.
 */
export const NAME_REVEAL_PASSWORD = 'dobbyisfree'

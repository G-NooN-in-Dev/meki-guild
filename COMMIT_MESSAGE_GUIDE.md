# 커밋 메시지 가이드

이 문서는 이 레포에서 사용하는 커밋 메시지 규칙을 정리합니다.

## 제목 형식

아래 두 형식 중 하나를 사용합니다.

- 프로젝트 작업: `[프로젝트명] type: 대표 내용`
- 공통 작업: `type: 대표 내용`

예시:

- `[my-app] feat: 메인 카드 UI 초기 구성`
- `chore: 공통 설정 파일 정리`
- `[my-app] !HOTFIX: 배포 전 긴급 오류 수정`

## 허용 type

- `feat`
- `fix`
- `docs`
- `refactor`
- `chore`
- `test`
- `style`
- `revert`
- `!HOTFIX`

## type 의미

- `feat`: 사용자 가치가 있는 기능을 추가할 때 사용합니다.
- `fix`: 버그를 수정하거나 잘못된 동작을 바로잡을 때 사용합니다.
- `docs`: README, 가이드, 주석 등 문서 성격의 변경에 사용합니다.
- `refactor`: 동작은 유지하면서 구조, 가독성, 유지보수성을 개선할 때 사용합니다.
- `chore`: 기능 변화 없이 설정, 정리, 의존성 관리 등 운영성 작업에 사용합니다.
- `test`: 테스트 코드 추가 또는 테스트 로직 보완에 사용합니다.
- `style`: 로직 변경 없이 포맷, 스타일, UI 표현을 다듬을 때 사용합니다.
- `revert`: 이전 커밋을 되돌리는 작업에 사용합니다.
- `!HOTFIX`: 배포 이슈 대응 등 즉시 반영이 필요한 긴급 수정에 사용합니다.

## 복합 type

- `type/type` 형태를 허용합니다.
- 예시: `feat/docs`, `docs/chore`, `refactor/test`

## 본문(상세 설명)

- 본문은 선택 사항입니다.
- 필요할 때만 아래처럼 항목을 추가합니다.

예시:

```text
[my-app] feat: 메인 카드 UI 초기 구성

- 메인 카드 레이아웃 추가
- 공통 색상 토큰 적용
- 모바일 대응 클래스 보완
```

## 권장 사항

- 제목은 짧고 핵심적으로 작성합니다.
- "무엇을"보다 "왜"를 드러내면 리뷰가 쉬워집니다.
- 한글/영문 모두 사용 가능합니다.
